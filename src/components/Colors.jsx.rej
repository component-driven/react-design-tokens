diff a/src/components/Colors.jsx b/src/components/Colors.jsx	(rejected hunks)
@@ -2 +2 @@ import * as React from "react";
-import styled from '@emotion/styled'
+import styled from "@emotion/styled";
@@ -5 +5 @@ interface Props {
-    foo?: boolean
+	foo?: boolean;
@@ -8,3 +8,6 @@ interface Props {
-const Colors = styled.div<Props>`
-  border: 1px solid ${props => props.foo ? 'red' : 'yellow'}
-`
+const Colors =
+	styled.div <
+	Props >
+	`
+  border: 1px solid ${props => (props.foo ? "red" : "yellow")}
+`;
@@ -13 +16 @@ const Colors = styled.div<Props>`
-export default Colors
+export default Colors;
